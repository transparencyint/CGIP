!!! 5
html
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge,chrome=1")
    meta(name="csrf_token", content=csrf_token())
    meta(name="viewport", content="width=device-width,initial-scale=1")
    link(rel="stylesheet", href="/stylesheets/app.css")
    script(src="/country-translation.js")
    script(src="/javascripts/vendor.js")
    script(src="/javascripts/app.js")
    if realtimePort
      script(src="http://speculos.taurus.uberspace.de:" + realtimePort + "/socket.io/socket.io.js")
    else
      script(src="/socket.io/socket.io.js")
    title CGIP
  body
    #outofsync

    .browserSupport
      h2 Please update your Browser! 
      p It doesn't support all necessary features. </br> Get a new version of Chrome or Firefox!
      div
        a.button(href='http://www.google.com/chrome') Get Chrome
        a.button(href='www.mozilla.org/de/firefox/') Get Firefox

    script(id="lm")
      window.lockedModels = !{JSON.stringify(lockedModels)};
    script(id="rp")
      window.realtimePort = !{realtimePort};

    if user
        script(id="user_hash")
          window.user_hash = !{JSON.stringify(user)};

    #container

    script
      require('initialize');