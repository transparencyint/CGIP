<h1><%= t('Import CSV') %></h1>

<div id="confirmInfo"><p><%= t('All in green marked lines have two matching actors and therefore will be added to the database.') %><br /><%= t('Please confirm the import by pressing the button.') %></p></div>
<div id="successInfo"><p><%= t('All changes successfully changed.') %></p></div>
<div id="failureInfo"><p><%= t('An error occurred. Changes are not saved.') %><br /><%= t('Check if you are logged in to save information.') %></p></div>
<input type="button" id="confirmButton" value="<%= t('Confirm Import') %>">

